{
  "name": "bbox-test-panel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "B-Box 移动 action 测试面板 —— Web Serial 直连 ESP32，复现推动检测逻辑，验证前后左右方向",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/w3c-web-serial": "^1.0.6",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
